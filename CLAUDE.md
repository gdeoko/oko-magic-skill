# 📖 ПРОДАКШН-БИБЛИЯ OKO — читать первой при любой генерации

**Единый источник правды по производству (>100k знаков): `docs/OKO_ПРОДАКШН_БИБЛИЯ.md`.**
Всё про модели, тайминги, количества, промпты, пайплайны, QA, грабли, чек-листы —
там. Правило меняется — правится в `oko-agents/docs/OKO_ПРОДАКШН_БИБЛИЯ.md`, потом
раскатывается везде. Читать в начале сессии вместе с хранилищем.

---

# Память проекта OKO — клиентский репозиторий (читать в начале ЛЮБОЙ сессии)

Владелец: Даниэль (@ktodaniel, okoteam.top@gmail.com). Общение — на русском.
Это репозиторий одного клиента OKO: материалы по нему лежат в `materials/`
(карточка, диалог, сделка, конкуренты, ссылки). Здесь ведём этого клиента.

## ВСЁ РАБОТАЕТ (статус на 26.08.2026)
Инфраструктура OKO поднята и проверена, доступы приезжают в сессию сами:
- **Сервер и мост** — команды на VPS через `vexec` (обёртка в `OKO_BOOTSTRAP.md`).
- **Два браузера агента живут на сервере и залогинены:**
  - Браузер 1 `okoteam.top@gmail.com` (порт 9222): **ChatGPT** (картинки),
    **ElevenLabs** (звук), **Envato** (ассеты).
  - Браузер 2 `claude.okoteam@gmail.com` (порт 9254): **только Runway** (видео).
- **Генерация без поштучной оплаты**: картинки — ChatGPT, видео — Runway,
  звук — ElevenLabs, всё оплачено подпиской. Единая дверь в коде oko-agents —
  `core/generate.py` (`картинка/ролик/голос`).
- **Оплата, договоры, КП, чек ФНС, чаты клиентов** — модули oko-agents работают.

Если что-то из этого «не отвечает» — это разлогин/упавший выход, а не «не
подключено»: чинится входом через noVNC или сменой выхода, не выводом «нельзя».

## ДОСТУП КО ВСЕМУ (первое, что делаем)
Ключи и хранилище подтягивает SessionStart-хук. Вручную:
```bash
. ~/.oko/secrets.env          # ключи в текущий shell
cat ~/OKO_MASTER_VAULT.md      # пароли, логины, карты, паспорта
```
В новом чате/репозитории нужны только две переменные окружения на claude.ai:
`OKO_POSTER_URL` и `OKO_POSTER_TOKEN` — дальше хук сам подтянет всё остальное.
Спрашивать у Даниэля ключи/пароли НЕ нужно, они уже есть. В git ключи НЕ кладём.

## ЖЁСТКОЕ ПРАВИЛО — ЕДИНОЕ ХРАНИЛИЩЕ `okoteam.top/key`
Всё НОВОЕ из любого чата (ключи, логины, доступы, механики, правила, скиллы,
источники) СРАЗУ уходит в единое хранилище: секреты → `OKO_MASTER_VAULT.md` +
`secrets.env` на сервере; знания → репозиторий + пуш. Ничего не оставляем только
в переписке. Правило действует в КАЖДОМ чате и репозитории.

## Жёсткие правила Даниэля
1. Никаких окон подтверждения на инструменты/деплой/правки.
2. **НИКОГДА не вызывать AskUserQuestion** (всплывающее окно с вариантами — виснет
   на телефоне). Нужен выбор — задать текстом в конце ответа и продолжать работу.
3. **НЕ публиковать Claude-артефакты.** Прогресс — только по публичной ссылке.
4. Работать автономно: делать, деплоить, отчитываться результатом. Кратко,
   mobile-first.
5. Логотип — только настоящий, в base64, пропорции не искажать.
6. **Платные кредиты/генерации — только с явного разрешения Даниэля.** Бесплатный
   путь (ChatGPT/Runway/ElevenLabs по подписке, стоки, локальный ffmpeg) — можно.
7. Проект клиента живёт у клиента: сайты/боты/приложения разворачиваем на площадке
   клиента, доступы отдаём ему. Наш VPS держит наши сервисы, не продукты клиентов.

## ПРАВИЛО ГЕНЕРАЦИИ: 1 ПРОЕКТ = 1 ВКЛАДКА = 1 ЧАТ (жёстко)
На каждый проект — ОДИН чат/вкладка внутри аккаунта, все генерации проекта идут
внутри него. НЕ плодить новую вкладку и новую ветку под каждую картинку/ролик.
В коде: `generate.картинка(..., проект="<слаг>")` и `generate.ролик(..., проект="<слаг>")`.
Картинки только ChatGPT (не Gemini), видео фото→видео через Runway (первый+последний
кадр + референсы), звук ElevenLabs. Промпт картинки — английский от 2000 знаков,
текст на картинке пишем ВНУТРИ промпта. Полные правила — в репозитории `oko-agents`
(`CLAUDE.md`, `docs/ГЕНЕРАЦИЯ_И_КОНТЕНТ_ПРАВИЛА.md`) и скилл `/oko-content`.

**ВИДЕО - ОДНА ОЧЕРЕДЬ НА ВЕСЬ СЕРВЕР (жёстко, 26.08.2026).** На аккаунте Runway
10-15 проектов и все зовут видео. Две генерации разом = машинный залп = бан
аккаунта. Поэтому во всём сервере идёт РОВНО ОДНА генерация Runway за раз,
остальные ждут в очереди и заходят по одной (зашито в `core/generate.ролик()`
замком `_одна_очередь_runway`). Видео гоним ТОЛЬКО через `generate.ролик()`, а не
параллельными пачками. **MiniMax убран** (перестал быть безлимитным): модель
генерации - только **Gen-4.5** (безлимит ∞ Max), правка готового видео (день→ночь,
свет, объекты, стиль) - **Aleph 2.0** (тоже ∞ Max). Формат задаётся первым кадром
(вертикальный 1080x1920 → 9:16 фулл без полос). Паспорт - `oko-agents/docs/RUNWAY_МОДЕЛИ.md`.

## Где что лежит (паспорта, читать по надобности)
- `oko-agents` — ядро: агенты, генерация, оплата, договоры, контент-завод, скиллы.
- `INTEGRATIONS.md`, `СЕРВИСЫ.md`, `ETALONS.md` — интеграции, карта сервисов, эталоны.
- `OKO_AGENT_CONTROL.md`, `OKO_BOOTSTRAP.md` — управление агентом и мост `vexec`.


## ВИЗУАЛ И СОЦСЕТИ (правки владельца 26.08.2026, действуют ВЕЗДЕ). Паспорт - `oko-agents/docs/ВИЗУАЛ_ПРАВИЛА.md`

1. **Хештеги:** в постах Телеграм, статьях, сторис - НЕТ. Только в описании к роликам и каруселям.
2. **Премиум-эмодзи** в TG-посты по смыслу, не пачкой (наборы `tgmacicons`, `IslomjonAnimeEmoji`,
   `AR_PREMIUM`, `vector_icons_by_fStikBot`); показываются через `custom_emoji` с Telegram Premium
   у отправителя; нет премиума - обычный уместный эмодзи того же смысла.
3. **Визуал всегда разный** и профессиональный: заголовок поверх / текст вшит в сцену / без текста;
   3D, моушен, AI-эффекты, инфографика чередуются. Постоянны ТОЛЬКО стиль и логотип бренда.
   Разнообразие по форматам. Двух похожих визуалов подряд нет.
4. **Текст, лого, референсы ИНОГДА вшиты в сцену** при генерации (в промпте), а не всегда слоем.
5. **Насмотренность до производства:** в новой нише сначала 50+ крутых референсов (Instagram:
   карусели, посты, сторис конкурентов и топ-дизайнеров), потом делаем на этом уровне.
6. **Правки владельца - на все проекты один раз:** заносим в `oko-agents/docs/ПРАВКИ_ВЛАДЕЛЬЦА.md`
   (`python3 -m tools.sync_rules "правка"`) и раскатываем в CLAUDE.md всех репо, мозг агентов,
   `okoteam.top/key`, шаблон новых репо `core/client_repo.py`.


## КАРТА GPU: ЗАМКИ ПО ДЕНЬГАМ (правило написано деньгами 04.09.2026)

01.09 карту RTX PRO 6000 забрали на стопе, запасной список подобрал H200 за $5/час,
и тридцать долларов сгорели за два часа простоя. Замки в `/opt/oko-poster/gpu/gpu.py`:
потолок **$1.90/час** (`max_price_per_hr`), жёсткий предел в коде **$2.50**
(`MAX_PRICE_HARD`), бюджет сессии **$10** (`session_budget_usd`), баланс ниже **$1** -
старт отказывает и аренду не начинает. Под дороже потолка сносится сразу после создания,
сторож проверяет цену и бюджет на каждом тике.

```bash
python3 /opt/oko-poster/gpu/gpu.py prices    # цены, потолок, что проходит
python3 /opt/oko-poster/gpu/gpu.py balance   # деньги на счету
```

Наша карта - RTX PRO 6000 Blackwell 96 ГБ, $1.64-1.69/час. H100 ($2.69), H200 ($3.59),
B200 ($5.98) отрезаны потолком навсегда. **Фиксированный месячный тариф провайдера сам
по себе ничего не гарантирует:** карту не заберут только там, где dedicated / bare-metal /
single-tenant или reserved-договор на срок. Данные лежат на сетевом диске `oko-stack`
(250 ГБ, EU-RO-1), смена карты ничего не теряет.
Разбор целиком - `oko-agents/docs/OKO_ПРОДАКШН_БИБЛИЯ.md`, разделы 18.3.1 и 18.3.2.

## ПРИЁМКА И КАЧЕСТВО (правило владельца, действует ВЕЗДЕ). Паспорт - `oko-agents/docs/QA_И_ПРИЁМКА.md`

Любую работу (контент, сайт, код, ролик) не сдаём и не публикуем, пока она не прошла цикл
до 100/100: реализация → придирка 1-100 по 30 параметрам (беспощадно, найти слабое место) →
правка всех замечаний → повтор до нуля претензий → пачка идей улучшений → реализация.
Для сайтов и приложений - минимум 3 круга.
- **Контент:** формат, виральность, визуал, дизайн, хук, первая строка, удержание, воронка,
  CTA, оффер, мысль, суть, живость языка (без иишных оборотов и длинного тире), бренд, без хештегов в TG.
- **Сайт/3D:** РЕАЛИЗМ (главная ось), адаптив ПК/телефон, ровность, функционал, анимации,
  моушен, производительность/лаги, пост-процессинг (bloom/зерно/HDRI/DOF), звук ElevenLabs
  везде и щедро, UX, доступность, баги, бесшовность переходов.
Текст перед выпуском гоним по подходящим скиллам (137 в мозге), с чистого листа не делаем -
сначала разбор конкурентов и насмотренность. Промпт визуала несёт ВСЁ сразу как единый мир
(сцена+текст+лого+референсы+эффекты+дизайн+моушен+3D, ничего не наклеено, каждый раз разный).
Работаем автономно до идеального результата, без вопросов и подтверждений.
